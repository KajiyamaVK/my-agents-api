meta {
  name: Chat Completion
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/llm/chat-completion
  body: json
  auth: bearer
}

auth:bearer {
  token: meta {
    name: Get Debug Resources
    type: http
    seq: 1
  }
  
  get {
    url: {{baseUrl}}/registry/debug
    body: none
    auth: bearer
  }
  
  auth:bearer {
    token: {{accessToken}}
  {{token}}
}

body:json {
  {
    "message": "Hi!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
